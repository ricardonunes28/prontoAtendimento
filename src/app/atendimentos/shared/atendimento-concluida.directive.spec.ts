import { AtendimentoConcluidaDirective } from './atendimento-concluida.directive';

describe('AtendimentoConcluidaDirective', () => {
  it('should create an instance', () => {
    const directive = new AtendimentoConcluidaDirective();
    expect(directive).toBeTruthy();
  });
});
